# Sprout — AI Farming Co-pilot — dependencies (all free / open-source)
# Core multi-agent framework (Google Agent Development Kit)
google-adk>=1.0.0

# Model Context Protocol (custom MCP server + client)
mcp>=1.2.0

# HTTP client for the free Open-Meteo weather API (no key required)
httpx>=0.27.0

# Env var loading
python-dotenv>=1.0.0

# ---- dev / test only ----
pytest>=8.0.0
pytest-asyncio>=0.23.0
